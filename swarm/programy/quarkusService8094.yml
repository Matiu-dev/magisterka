version: '3.8'

services:
  nbp-app8094:
    image: matiuw/nbp-app8094:latest
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      restart_policy:
        condition: any
    ports:
      - "8094:8094"
    networks:
      - NBP-APP

networks:
  NBP-APP:
    external: true

